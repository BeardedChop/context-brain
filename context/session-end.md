# Session End

Before ending a session:

1. Write what changed
2. Write what is still unfinished
3. Write any risky assumptions
4. Update `docs/DECISIONS.md` if structure changed
5. Update `docs/CONTEXT.md` with the new current state
6. Run `scripts/context-pack.sh` so the next session starts cleanly
