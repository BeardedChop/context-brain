# Session End

Before ending:

1. Write which endpoint, job, or service changed
2. Note unfinished validation, persistence, or error handling
3. Update `docs/DECISIONS.md` if boundaries changed
4. Update `docs/CONTEXT.md` with the next endpoint/job task
5. Re-run the context packer
