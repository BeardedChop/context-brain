# Session End

Before ending:

1. Write which command changed
2. Note unfinished input, output, or error behavior
3. Update `docs/DECISIONS.md` if command boundaries changed
4. Update `docs/CONTEXT.md` with the next command task
5. Re-run the context packer
